import clsx from "clsx";

// TODO: 회원 관리(SB-06 STEP 2) 화면으로 교체 — 목록/차단 API 는 service/user·admin 에 준비되어 있다
function AdminUsers() {
    return (
        <div className={clsx("flex", "h-full", "items-center", "justify-center")}>
            <p>회원 관리</p>
        </div>
    );
}
export default AdminUsers;
